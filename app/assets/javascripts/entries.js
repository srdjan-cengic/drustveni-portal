// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
$(function() {
	//$('#dropdown_index').change(function() {
		// this = dropdown
		// $(this).text() => vraca kompletan tekst iz dropdown, NE samo ono sto je selektovano
		// console.log($('#dropdown_index option:selected').text()); OVO vraca samo selektovani tekst

		//$.ajax({
			//type: 'get',
			//data: { id: this.value },
			//url: '/entries/new'
		//});
	}
);

